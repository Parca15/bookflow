spring:
  application:
    name: backend

  profiles:
    active: dev

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:bookflowSecretKeyForJwtTokenGeneration2024BookFlowSystemManagement}
  expiration: 86400000

cors:
  allowed-origins: ${CORS_ORIGINS:http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173}